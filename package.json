{
  "name": "stratum-frame",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free WebGPU structural frame modeling and finite-element analysis workbench",
  "scripts": {
    "start": "node serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node tests/verification.mjs",
    "build": "node tools/build-single.mjs",
    "build:pages": "npm run build && node tools/build-pages.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
